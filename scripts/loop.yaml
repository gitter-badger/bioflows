id: loopwf
name: loopwf
type: pipeline
steps:
  - id: loop
    name: loop
    type: tool
    loop: true
    loop_var: "myarray"
    inputs:
      - id: myarray
        name: myarray
        type: array
    command: "mkdir {{self_dir}}/{{myarray_item}} && echo {{myarray_item}}"